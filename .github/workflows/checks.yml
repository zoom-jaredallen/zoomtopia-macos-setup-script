name: Validate app
on: [push, pull_request]
permissions:
  contents: read
jobs:
  validate:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test and validate shell syntax
        run: |
          bash -n Scripts/*.sh Tests/bootstrap-test.sh
          ./Scripts/test.sh
      - name: Build universal app
        run: ./Scripts/build-app.sh
      - name: Verify bundle
        run: |
          lipo -verify_arch x86_64 arm64 "dist/Zoomtopia Setup.app/Contents/MacOS/Zoomtopia Setup"
          lipo -verify_arch x86_64 arm64 "dist/Zoomtopia Setup.app/Contents/Resources/PayloadVerifier"
          codesign --verify --deep --strict "dist/Zoomtopia Setup.app"
          plutil -lint "dist/Zoomtopia Setup.app/Contents/Info.plist"
